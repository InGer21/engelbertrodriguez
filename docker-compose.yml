services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    volumes:
      - storage-data:/var/www/html/storage
    environment:
      - APP_ENV=production
      - APP_DEBUG=false
      - APP_KEY=base64:8J9ycCCFqV4VqBjSHqHsFtGbPKhfm41NqvnWf3kced8=
      - DB_CONNECTION=sqlite
      - APP_URL=http://localhost:8080

volumes:
  storage-data:
